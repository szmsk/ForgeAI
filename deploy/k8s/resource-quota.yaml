apiVersion: v1
kind: ResourceQuota
metadata:
  name: forgeai-sandbox-quota
  namespace: forgeai-sandbox
spec:
  hard:
    requests.cpu: "8"
    requests.memory: 16Gi
    limits.cpu: "16"
    limits.memory: 32Gi
    count/jobs.batch: "50"
